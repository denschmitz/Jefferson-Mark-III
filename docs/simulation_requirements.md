# Simulation Requirements

The simulation requirements have been split into smaller files for maintainability.

Use [docs/simulation/requirements/index.md](simulation/requirements/index.md) as the canonical entry point. Requirement IDs are unchanged.
